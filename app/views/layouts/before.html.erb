<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <% if params[:state] == nil %>
      <meta http-equiv="refresh" content="<%= " 2; url=/date_plans/#{liff.state.slice(liff.state.length - 2 , 2).gsub(/[^\d]/, "")}/detail" %>">
    <% else %>
      <meta http-equiv="refresh" content="<%= " 2; url=/users/my_page" %>">
    <% end %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <title>マッチングアプリマスター</title>
  </head>
  <body>
    <%= yield %>
    <%= javascript_pack_tag 'before', 'data-turbolinks-track': 'reload' %>
    <script charset="utf-8" src="https://static.line-scdn.net/liff/edge/2/sdk.js"></script>
  </body>
</html>
